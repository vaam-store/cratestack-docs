# cratestack-docs

Mintlify-ready documentation project for the framework being prepared for public release as `CrateStack`.

Current contents:

1. `mint.json` site navigation
2. `docs/` content prepared for a standalone docs site

The framework code still lives under `coolstack/` for now. This project exists so the docs site can evolve independently before the framework itself is extracted and renamed.
